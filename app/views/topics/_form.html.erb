<%= form_for(@topic, url: choose_new_or_edit) do |f| %>

  <%= f.error_messages! %>

  <%= f.label :title %>
  <%= f.text_field :title %>
  <br>
  <%= f.label :content %>
  <%= f.text_field :content %>
  <br>
  <%= f.submit %>
<% end %>

<%= link_to "一覧画面にもどる", topics_path %>
